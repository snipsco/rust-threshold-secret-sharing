extern crate threshold_secret_sharing as tss;

fn main() {
    //
    // use tss::ShamirSecretSharing;
    // // use tss::MultiThresholdSecretSharing;
    //
    // let prime = 41;
    // let threshold = 5;
    // let shares = 20;
    // let tss = tss::ThresholdSecretSharing::new(shares, threshold, prime);
    //
    // let secrets = vec![1, 2, 3];
    // let shares = tss.share(secrets);
    //
    // let recon_secrets = tss.reconstruct(shares);
    //
    // println!("{:?}", recon_secrets);

    // let reconstructed_secret = tss.reconstruct(&shares);
    // assert_eq!(1, reconstructed_secret);
}
